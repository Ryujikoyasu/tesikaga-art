# This is a dummy calibration file for initial testing.
# It assumes the LiDAR coordinate system is perfectly aligned with the world coordinate system.
# The transformation_matrix is an identity affine matrix.
#
# Generated by the assistant for placeholder purposes.

transformation_matrix:
  - - 1.0  # a (scale x)
    - 0.0  # b (shear y)
    - 0.0  # tx (translate x)
  - - 0.0  # c (shear x)
    - 1.0  # d (scale y)
    - 0.0  # ty (translate y)

lidar_pose_world:
  x: 0.0
  y: 0.0
  theta_rad: 0.0
  theta_deg: 0.0